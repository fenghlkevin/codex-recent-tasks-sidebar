# Agent Instructions

完整阅读并遵循 `skills/codex-recent-tasks-sidebar/SKILL.md`。

优先复用仓库内模板和脚本，不要从零重写。交付前运行 `./scripts/qa.sh`，再在真实 macOS 桌面环境检查核心窗口交互。不得读取、打印、复制、提交或上传真实 Codex 任务内容和数据库。覆盖 `/Applications` 前必须获得用户授权。
